{
  "name": "kimi-coding-agent-notes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name kimi-coding-agent-notes",
    "check": "npm run build && node scripts/check.mjs"
  },
  "dependencies": {
    "github-slugger": "2.0.0",
    "marked": "18.0.7",
    "mermaid": "11.16.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "wrangler": "4.114.0"
  },
  "engines": {
    "node": ">=22"
  }
}
